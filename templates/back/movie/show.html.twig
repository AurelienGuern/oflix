{% extends 'back/layout_back.html.twig' %}

{% block title %}Movie{% endblock %}

{% block body %}
    <h1>Movie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ movie.id }}</td>
            </tr>
            <tr>
                <th>ReleaseDate</th>
                <td>{{ movie.releaseDate ? movie.releaseDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ movie.duration }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ movie.title }}</td>
            </tr>
            <tr>
                <th>Poster</th>
                <td>{{ movie.poster }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ movie.rating }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ movie.type }}</td>
            </tr>
            <tr>
                <th>Summary</th>
                <td>{{ movie.Summary }}</td>
            </tr>
            <tr>
                <th>Synopsis</th>
                <td>{{ movie.Synopsis }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_movie_index') }}" class="btn btn-info" >retour à la liste</a>

    <a href="{{ path('app_back_movie_edit', {'id': movie.id}) }}  class="btn btn-success">modifier</a>

    {{ include('back/movie/_delete_form.html.twig') }}
{% endblock %}
