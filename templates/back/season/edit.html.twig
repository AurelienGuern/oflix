{% extends 'back/layout_back.html.twig' %}

{% block title %}Modification saison{% endblock %}

{% block body %}
    <h1>Modification saison de la série {{ movie.title }}</h1>

    {{ include('back/season/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_back_season_index', {'id': movie.id}) }}">back to list</a>

    {{ include('back/season/_delete_form.html.twig') }}
{% endblock %}
