	<div class="d-flex mb-4" style="color: orange;">
							 {# on veux parcourir de <i> movie.rating fois #}
{# on initalise le reste au rating #}
{% set reste = movie.rating %}							 
{% for i in range(1, movie.rating|round(0, 'floor')) %}
    <i class="bi bi-star-fill"></i>
	{# on le décrémente à cahque passage #}
	{% set reste = reste - 1 %}
{% endfor %}
{# lorsqu'on sort, s'il y a un reste "suffisant" on affiche une demi étoile #}
{% if reste > 0.3 %}
	<i class="bi bi-star-half"></i>
{% endif %}